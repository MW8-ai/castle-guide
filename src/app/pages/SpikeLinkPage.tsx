export function SpikeLinkPage() {
  return (
    <section class="page">
      <h1>Isometric spike</h1>
      <p>
        The throwaway contract spike lives at{' '}
        <code>spikes/iso-canvas</code>. Run it with:
      </p>
      <pre class="code-block">npm run spike:iso</pre>
      <p class="muted">
        It renders two rooms from real dimension data and one clickable
        appliance placement. No polish — proof only.
      </p>
    </section>
  );
}
