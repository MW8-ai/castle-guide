# `src/council`

## Owns

- Character definitions UI, contextual nugget surfacing, name overrides

## Must never

- Own the underlying knowledge truth (JSON in `/data` does)
- Invent savings numbers without content fields
