# `src/ai`

## Owns

- BYO-key adapters + manual Prompt Pack mode
- Validate LLM output against schemas before import

## Must never

- Require a key for core features
- Persist keys outside the browser or into default exports
- Commit secrets
