# iso-canvas spike

Throwaway proof for ADR-0002 / renderer plugin contract.

## Run

```bash
npm run spike:iso
```

## Acceptance

- Two rooms drawn from `dims.L` × `dims.W` (feet)
- One appliance placement with footprint
- Click → `onSelectItem(itemId)` (see console + on-screen log)
- No storage writes

Not product UI. Do not polish.
